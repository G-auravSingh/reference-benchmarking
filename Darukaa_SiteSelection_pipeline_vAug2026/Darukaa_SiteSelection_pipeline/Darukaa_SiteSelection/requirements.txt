# Darukaa_SiteSelection pipeline dependencies
# Generated by grepping every import statement actually used across
# pipeline/**/*.py, not written from memory — this file did not exist
# before (a real gap, flagged directly, Aug 2026).
#
# Install with: pip install -r requirements.txt
# (On some systems, e.g. externally-managed Python installs, you may need:
#  pip install -r requirements.txt --break-system-packages)

geopandas>=0.14
shapely>=2.0
pyproj>=3.6
pyyaml>=6.0
scikit-learn>=1.3
numpy>=1.24
pyogrio>=0.7          # geopandas' shapefile read/write backend — required
                       # for common/gee_export.py's Shapefile export; without
                       # this OR fiona installed, gpd.read_file/to_file for
                       # Shapefile will fail at import/runtime, not install time.
openpyxl>=3.1          # .xlsx export for the deployment schedule
